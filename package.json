
{
  "name": "follow-hq-monolith",
  "version": "1.0.0",
  "description": "Interface et Serveur FollowTrend combinés",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "path": "^0.12.7"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
